import { List } from 'antd';

const ListUsers = (props) => {

  // state = {
  //   title: [],
  //   color: [],
  //   id: [],
  // }



  const booking = (id) => {
    console.log(id)
  }

  const noBooking = () => {}



    return (
      <>
        {/* <List
          dataSource={props}
          renderItem={item => (
            <List.Item>
              <a >{item.title}</a>
            </List.Item>
          )}
        /> */}
      </>
    )
}

export default ListUsers;

// style={{color: `${}`}} 